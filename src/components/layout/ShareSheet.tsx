import { View } from 'react-native';
import { Text } from '@/components/StyledText';

/** Placeholder for share sheet (product, video, etc.). Phase 0. */
export function ShareSheet() {
  return (
    <View style={{ padding: 16 }}>
      <Text style={{ fontSize: 14, opacity: 0.7 }}>Share — coming soon</Text>
    </View>
  );
}
